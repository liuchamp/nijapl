# TTS 服务 base URL（构建期经 `lynx.config.ts` 的 `source.define` 注入为 `__TTS_BASE_URL__`）。
#
# 未设置时，运行期回落到 `src/constants/tts.ts` 中的本机调试默认值 `http://127.0.0.1:8000`。
# 生产：`TTS_BASE_URL=https://tts.<domain> npm run build`
#
# 说明：业务代码一律 `import { TTS_BASE_URL } from '../constants/tts.js'`，
# 禁止出现 IP / 域名字面量（唯一例外为 `src/constants/tts.ts` 的调试默认值与本文件）。
TTS_BASE_URL=http://127.0.0.1:8000
